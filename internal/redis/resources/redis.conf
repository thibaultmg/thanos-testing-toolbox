# Example redis.conf with ACL

# General Redis settings
bind 127.0.0.1
protected-mode yes
port 6379

# ... other configurations ...

# ACL configuration
# aclfile /path/to/your/aclfile.acl

# Alternatively, you can define users directly in redis.conf:
user thanosuser on +@all >thanospassword
